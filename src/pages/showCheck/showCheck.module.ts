import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ShowCheckPage } from './showCheck';

@NgModule({
  declarations: [
    ShowCheckPage,
  ],
  imports: [
    IonicPageModule.forChild(ShowCheckPage),
  ],
})
export class ShowCheckPageModule {}
